# TODO

本清单用于安排后续 AI 和人工协作。产品范围与电话状态定义以
`docs/callback-demo-plan.md` 为准；每完成一项，同时更新测试和本清单。

## 产品定位（2026-08 调整）

本系统是**运营支撑助手**：通话（后续加短信）只用来**通知工作人员**去处理
工作，不是通知客户本人。

- 通知对象：业务维护人（客户经理）、网络维护责任人、审核人员。
- 核心场景：① 协议到期前提醒客户经理维系；② 退网后提醒网络维护责任人回收
  设备；③ 审核卡单时提醒审核人员。
- 话术是固定模板 + 数据自动填充；第一版通知通道为电话（A7670E + 本地播放），
  短信同步通知列为后续能力。
- 因此**不再需要管理员手动设置"客户回访计划"**——通知任务由每日固定扫描
  （时段可配置）自动生成。

## 已完成基线（历史归档）

- [x] 后台骨架（FastAPI + Jinja2 + SQLite + SQLAlchemy）、单密码登录、
  客户/话术/回访计划/队列/通话记录基础页面。
- [x] `once` / `cron` 计划时间计算、到期入队、暂停调度、过期单次任务标记；
  自动重试（可追踪 attempt）与重启恢复（不补打历史、回收遗留 dialing 任务）。
- [x] A7670E 串口客户端、事件解析器、单通道 CallWorker（queued→dialing→
  connected→completed/short_call，未接通/拒接/忙线/超时/播放失败分类，
  AT+CHUP 清理，事件逐条落库），`CALL_WORKER_ENABLED` 默认关闭的硬开关。
- [x] 硬件闭环人工验收：串口握手、aplay/ffmpeg 播放、smoke test、
  应用级端到端（15 条 CallEvent 落库）、对端听音。
- [x] Alembic 迁移体系（初始全量 schema + 种子字典/权限/五角色）、
  `uv` 依赖管理、schema 版本启动校验。
- [x] 台账导入与审核链路：XLSX 扁平行导入暂存（3,113 行集成验证：317 阻断 /
  2,796 缺项 / 107 归属冲突）、表头别名、多设备拆分、暂存更正重校验、
  中文导出（含稳定 ID/版本，防覆盖冲突）、business/network 双域审核
  状态机、字段级差异、退回原因、版本检查与事务回滚。
- [x] 用户/角色管理、密码哈希、基于角色与业务域的权限拦截、
  系统管理（监控/日志/设置/备份）与 Web 审计日志。
- [x] P1 后台操作体验（通话筛选分页/详情时间线/人工重入队、计划 cron 预览、
  仪表盘 Worker 状态、高影响操作确认）与系统监控鉴权。
- [x] 备份与灾备：SQLite backup API 一致性快照 + `data/imports/` + `data/audio/`
  打包，本地保留 + WebDAV 远端上传，保留期管理，手动触发/下载/历史。
- [x] TTS：`TTS_PROVIDER=edge`（Edge 在线 TTS）/ `none`；音频目录/命名/缓存
  规范，`tts_error` 失败状态与重试、页面试听；播放用系统 ffmpeg 输出 ALSA。
- [x] Rock Pi 3A 部署文档（docs/deploy-rockpi.md）与 systemd 示例。
- [x] 2026-08 精简：删除 `custom_field_definitions` / `custom_field_values`
  两张 0 数据预留表；删除 `call_records` 的 AI/录音占位列（recording_path、
  transcript_text、summary_text、sentiment、follow_up_required、
  ai_result_json）；删除 /customers 客户页（客户主体由台账导入审核链路
  自动创建）；通讯录回归独立目录（/contacts，仅人名+电话+职责绑定）；
  data/app.db 清空全部测试/示例数据（保留字典/权限/角色种子）。

## 待办：台账上线演练（人工）

- [ ] 上线前人工处理示例台账中的 317 行阻断错误和 107 行设备归属冲突，复核
  缺项报告，并执行导出、回传、审核、应用、备份恢复全流程演练。
- [ ] 在真实 `.env` 中设置强 `ADMIN_PASSWORD`（上线前人工轮换）。

## P0 剩余：硬件闭环补验（人工）

- [ ] 未接通、对端挂断、播放失败和连接超时后的实机释放验证（fake serial
  单测已覆盖，实机待验）。
- [ ] 记录 TRRS 引脚、音量、声卡设备和可复现接线方案；若直连失败，评估转接、
  衰减、隔直或混音硬件。

## 运营支撑助手：到期维系与设备回收通知（核心）

### 1. 协议到期维系扫描（通知客户经理）

- [x] 每日固定时间（默认 09:00，可配置）扫描 `business_services` 中协议到期
  前 N 天（默认提前 14 天）的业务（`ScanSchedule.scan_type=due_renewal`）。
- [x] 按业务维护人（客户经理，`customer_contacts` 职责「客户经理」）生成通知
  任务（source=`due_renewal`）；同业务同日内不重复通知（meta_json 去重）。
- [x] 话术模板：`scan_schedules.script_id` 可选模板 + 内置默认话术；正文支持
  `{{客户名称}}` 等占位符，渲染后新建 Script 并生成音频。
- [x] 续签闭环：客户经理提交「修改协议到期时间」变更申请 → 审核 → 应用
  （/due-work 工作台续签申请，审核通过后 `agreement_expires_at` 更新）。
- [x] 退网闭环：不续签时客户经理提交「业务退网」变更申请 → 审核 → 应用
  （服务状态改为「主动退网(申请拆机)」字典项）。
- [x] 重复出现处理（决策）：不加统计字段——去重是同日级别，跨日自然再次
  通知；状态改变（续签/退网/回收审核应用）后窗口判定失效，不再提醒；
  工作台展示累计通知次数（`call_tasks` 的 `meta_json` 按业务/设备统计）。
- [x] 工作台防重复申请：业务/设备已有 submitted 或 approved（未应用）申请
  时不再显示申请按钮，改为展示「已有申请待审核」；已退网业务只保留回收
  入口，已回收设备不再显示回收按钮。

### 2. 退网设备回收扫描（通知网络维护责任人）

- [x] 每日固定时段扫描「退网/不续约」业务（字典「退网」标签或协议已过期）下
  未回收设备（`recovery_status` 为空或未完成），通知网络维护责任人
  （source=`device_recycle`）；同设备同日去重。
- [x] 回收闭环：回收完成后提交变更（设备回收状态「已回收」）走审核
  （/due-work 设备回收申请）；未回收的持续每日提醒，回收完成并审核通过后
  不再出现。

### 3. 话术模板与自动填充

- [x] 话术正文支持占位符：`{{客户名称}}`、`{{业务号码}}`、`{{协议到期日}}`、
  `{{负责人姓名}}`、`{{设备编码}}` 等；渲染后保存快照（meta_json
  `rendered_script`）并生成音频。
- [x] 短信与通话共用同一话术模板源（短信内容 = 渲染后话术文本，ASCII/UCS2
  发送见下方「短信同步通知」）。

### 4. 扫描时段配置（取代手动回访计划）

- [x] 删除「回访计划」页面与路由，不再允许按客户手动建计划（plans.html 删除，
  `/scan-schedules` 新增，导航「扫描通知」）。
- [x] `callback_plans` 基础上改造为系统级扫描配置（`scan_schedules`）：扫描
  类型（到期维系 / 设备回收）、每日时段或每周几（cron）、时区、提前天数、
  启用/停用；调度器每分钟按 cron 匹配触发。
- [x] `call_tasks` 保留 `plan_id` 可空，新增 `scan_schedule_id` 与 `meta_json`
  快照；任务来源由 `source` 区分（`due_renewal` / `device_recycle` / `review_stuck` / `manual`）。

### 5. 审核卡单提醒

- [x] 扫描类型 review_stuck：每次 cron 到点把全部 submitted 待审核的变更
  申请扫出来，通知有 review 权限且有手机的启用用户；同卡单同日去重
  （不做卡单时长阈值配置，审核通过/驳回后状态变化自然停止提醒）。
- [x] 扫描配置表单：review_stuck 类型时「提前天数」无意义，页面已加说明提示。

### 6. 用户实名与按步骤通知

- [x] `users` 表增加 `real_name`、`phone`；创建用户时实名必填、手机必填
  （系统管理员可不设手机），创建/编辑都校验。
  - [x] 台账导入应用时按职责自动创建登录账号（与通讯录打通）：
  - 审核应用时按「客户经理 / 网络维护责任人」自动建号（暂存/导入阶段不建号）；
  - 用户名 = 手机号（唯一），初始随机密码，首次登录强制改密；
  - 同名/同号联系人已有账号时复用绑定，不重复建；通讯录仍保留为未建号
    联系人的兜底通知来源。

### 7. 短信同步通知（A7670E 直发，无需运营商网关）

- [x] 短信能力真机验证：SIM READY、CSQ 24、CMGF=1 文本模式 + IRA 字符集
  ASCII 发送成功（+CMGS: 1），测试号 15509255433 端到端收到；串口沿用
  `.env` 配置（ttyUSB1，ttyUSB2 备用）。
- [ ] 短信通知落地（进行中）：`sms_notifications` 记录表 + 扫描配置
  `sms_enabled` 开关 + `SMS_ENABLED` 硬开关；扫描生成任务时同步入队短信，
  CallWorker 空闲时串行发送（单通道与语音互斥，发送前查 CPIN/CSQ、
  等 `>` 提示符、URC 行跳过、UCS2 中文编码、失败记录不阻塞语音）。
- [ ] 短信记录页（/sms：时间/脱敏号码/内容摘要/状态/错误）。

### 0. 定位落地与通知通道

- [x] 更新 README 与 docs：定位从「电话回访 demo」升级为「运营支撑助手」。
- [x] 短信能力验证与落地规划（见「7. 短信同步通知」）；运营商短信网关/
  邮件/企微等外部通道仍为后续。

## 明确延期

- [ ] ASR、实时转写和录音（已删除相关占位列，除非产品范围明确调整）。
- [ ] LLM 实时对话、抢断和动态话术。
- [ ] DTMF 按键反馈。
- [ ] 节假日、工作日历和复杂补偿策略。
- [ ] 公网部署安全加固；当前多用户、角色和业务域权限仅面向内网使用。
- [ ] 运营商短信网关/邮件/企微等外部通知通道（A7670E 直发短信已落地，
  外部通道均为后续）。
