# TODO

本清单用于安排后续 AI 和人工协作。产品范围与电话状态定义以
`docs/callback-demo-plan.md` 为准；每完成一项，同时更新测试和本清单。

## 当前基线

- [x] 使用 FastAPI、Jinja2、SQLite 和 SQLAlchemy 搭建后台骨架。
- [x] 实现单密码登录和签名 Session Cookie。
- [x] 实现客户、话术、回访计划、队列记录和通话记录的数据模型与基础页面。
- [x] 实现 `once` / `cron` 下一次运行时间计算、到期入队和过期单次任务标记。
- [x] 实现全局调度启用/暂停设置。
- [x] 实现 A7670E 串口客户端、基础事件解析器和解析器单元测试。
- [x] 提供人工硬件 smoke test 和本地 WAV 播放封装。
- [x] 迁移到 `uv`，以 `pyproject.toml` 和 `uv.lock` 管理依赖。

## P0：先完成硬件闭环

- [ ] 在真实 `.env` 中设置强 `ADMIN_PASSWORD` 和随机 `SESSION_SECRET`。
- [ ] 核对 A7670E 实际 AT 串口，并确认 `MODEM_PORT` 配置正确。
- [ ] 用 `aplay -l` / `aplay -L` 核对 3.5mm 输出设备，并确认 `AUDIO_DEVICE`。
- [ ] 准备格式兼容、音量合适的测试 WAV 文件。
- [ ] 人工运行 `uv run python scripts/hardware_smoke.py <phone> <wav>`，确认能观察到
  `VOICE CALL: BEGIN`，且对端能稳定听到完整音频。
- [ ] 验证未接通、对端挂断、播放失败和连接超时后串口及通话均能正确释放。
- [ ] 记录 TRRS 引脚、音量、声卡设备和可复现接线方案；若直连失败，评估转接、
  衰减、隔直或混音硬件。

## P0：完成单通道 Call Worker

- [ ] 将 `ModemClient`、事件解析器和音频播放接入 `CallWorker`。
- [ ] 实现 `queued -> dialing -> connected -> completed/short_call` 的成功路径。
- [ ] 实现 `no_answer`、`rejected`、`cancelled_or_failed`、`busy`、`failed` 分类。
- [ ] 使用 `CALL_CONNECT_TIMEOUT_SECONDS` 做应用层接通超时兜底。
- [ ] 按 `MIN_CONNECTED_SECONDS` 判定 `short_call`，优先保存模块报告的时长。
- [ ] 把 AT 命令、原始响应、URC、播放开始/结束、挂断和异常逐条写入
  `CallEvent`。
- [ ] 确保异常和进程停止时尝试执行 `AT+CHUP`，并正确关闭串口和播放器。
- [ ] 保证同一时刻只领取一个任务，避免 Web 多进程或重复 Worker 并发拨号。
- [ ] 增加显式 `CALL_WORKER_ENABLED` 配置，默认关闭；只有确认硬件后才允许自动
  启动 Worker。

## P0：重试与恢复

- [ ] 对可重试的未接通结果最多重试一次，延迟使用 `RETRY_DELAY_SECONDS`。
- [ ] 明确哪些状态可重试，避免对已接通短通话或人工终止进行自动重拨。
- [ ] 为每次尝试保留独立、可追踪的任务/记录关系和 attempt 信息。
- [ ] 测试重启后 `once` 任务标记为 `missed`，`cron` 不补打停机期间的历史次数。
- [ ] 处理 Worker 启动时遗留在 `dialing` / `connected` 的任务，避免永久卡住。

## P1：测试与可靠性

- [ ] 为计划时间计算、无效 cron、时区和夏令时边界增加单元测试。
- [ ] 为到期入队、暂停调度、立即拨打和重启恢复增加临时 SQLite 测试。
- [ ] 为 Call Worker 增加 fake serial 和 fake audio 测试，覆盖成功、忙线、拒接、
  超时、播放失败和挂断。
- [ ] 增加登录保护、客户/话术/计划表单校验和主要页面 smoke test。
- [ ] 修正删除被计划或通话记录引用的客户/话术时的用户可见错误处理。
- [ ] 校验 `SESSION_SECRET`，避免真实部署时为空或继续使用示例值。
- [ ] 增加结构化运行日志，并避免记录完整密钥和不必要的完整手机号。

## P1：后台操作体验

- [ ] 在通话列表增加状态、日期、客户筛选和分页。
- [ ] 在通话详情完整展示时间线、原始串口行和错误信息。
- [ ] 在计划表单中提供更清晰的 cron 校验错误和下一次执行时间预览。
- [ ] 补全从客户或计划发起“立即拨打一次”的一致入口和确认提示。
- [ ] 提供失败任务的人工重新入队操作，不修改原 cron 计划。
- [ ] 在仪表盘显示 Worker 是否启用、串口是否可用及当前通话状态。
- [ ] 对删除、拨号、重拨等高影响操作增加明确确认和结果反馈。

## P2：音频与部署

- [ ] 接入一个真实 TTS Provider，并保留 `TTS_PROVIDER=none` 作为离线默认值。
- [ ] 规范生成 WAV 的目录、命名、采样率、声道和缓存/覆盖策略。
- [ ] 增加音频生成失败状态、重试入口和页面试听能力。
- [ ] 编写 Rock Pi 3A 部署文档，包括串口权限、声卡选择、`uv sync` 和服务启动。
- [ ] 提供 systemd 服务示例，区分 Web/Scheduler 与硬件 Worker 的启动开关。
- [ ] 增加 SQLite 备份、恢复和通话记录导出方案。

## 明确延期

- [ ] ASR、实时转写和录音。
- [ ] LLM 实时对话、抢断和动态话术。
- [ ] DTMF 按键反馈。
- [ ] 节假日、工作日历和复杂补偿策略。
- [ ] 多管理员、角色权限和公网部署安全加固。

以上项目不进入最小 Demo，除非产品范围被明确调整。

## 最小 Demo 验收

- [ ] 管理员可登录并管理客户、话术和 `once` / `cron` 回访计划。
- [ ] 到期计划只生成一次正确排序的任务，暂停调度时不生成新任务。
- [ ] 单通道 Worker 能拨号、识别接通、播放 WAV、挂断并释放资源。
- [ ] 成功、短通话、未接通、忙线、超时和播放失败均有正确主状态。
- [ ] 每次通话可在后台查看完整时间、时长、错误和串口事件。
- [ ] 自动重试不超过配置上限，服务重启不会补打历史任务。
- [ ] 自动化测试通过，真实硬件链路由操作者完成一次端到端验收。
