# TODO

本清单用于安排后续 AI 和人工协作。产品范围与电话状态定义以
`docs/callback-demo-plan.md` 为准；每完成一项，同时更新测试和本清单。

## 当前基线

- [x] 使用 FastAPI、Jinja2、SQLite 和 SQLAlchemy 搭建后台骨架。
- [x] 实现单密码登录和签名 Session Cookie。
- [x] 实现客户、话术、回访计划、队列记录和通话记录的数据模型与基础页面。
- [x] 实现 `once` / `cron` 下一次运行时间计算、到期入队和过期单次任务标记。
- [x] 实现全局调度启用/暂停设置。
- [x] 实现 A7670E 串口客户端、基础事件解析器和解析器单元测试。
- [x] 提供人工硬件 smoke test 和本地 WAV 播放封装。
- [x] 迁移到 `uv`，以 `pyproject.toml` 和 `uv.lock` 管理依赖。
- [x] 引入 Alembic：初始迁移建立完整新 schema（回访 + 用户权限 + 业务台账 +
  设备 + 联系人 + 字典 + 变更申请 + 暂存导入 + 审计），种子迁移预置字典、
  权限和五个岗位角色；旧开发库已丢弃重建，应用启动改为校验 schema 版本。
- [x] 2026-08 推进（herdr + codex/pi 多 agent 协作）：完成 P0 重试恢复（遗留
  任务回收 + 重启不补打）、P1 测试可靠性（154 项全绿）、后台操作体验、
  系统监控鉴权、P1 备份与灾备（SQLite/WebDAV）、部署文档与 systemd 示例、
  音频目录规范与试听。

## 后续：台账导入与审核链路

- [x] 实现扁平台账导入：上传 XLSX、A-W 字段解析、类型/重复/缺项/归属冲突
  校验，原始行和批次号写入暂存表（`import_batches` / `staging_rows`）。
- [x] 支持表头别名及括号说明匹配，例如“设备编码（资产及非资产）”映射到
  “设备编码”；多个候选时阻止自动猜测。
- [x] 支持同一业务多设备扁平行，以及按顿号、中文/英文逗号、分号和换行拆分
  设备编码；无法确定的分隔和跨业务设备归属进入异常清单。
- [x] 缺项/异常暂存行可在导入报告中人工更正并重新校验，同时保留最初上传值。
- [x] 导出使用中文 `业务设备台账` 数据工作表，不再生成英文规范化 Sheet 或
  `dictionary_snapshot`；每行表示一个业务与一个设备的关联，并附中文 `填写说明`。
- [x] 导出附带业务/设备稳定 ID 和记录版本，支持本地修改后重新提交；这些系统校验
  字段明确标记为“请勿修改”，不保留无实际语义的冗余“操作”列。
- [x] 在填写说明、表头批注和 Excel 下拉校验中写明固定枚举、日期/电话格式、设备
  编码分隔符及一行一台设备的推荐规则。
  未变化记录不生成变更项，版本变化时阻止覆盖并报告冲突。
- [x] 导入变更按 `business` / `network` 拆成独立审核申请，分别完成审核和应用；
  两域申请全部应用后，导入批次才标记为已应用。
- [x] 实现审核状态机、字段级差异、退回原因、应用时版本检查和事务回滚。
- [x] 用户、角色管理页面与登录升级（密码哈希、启用/禁用、角色绑定、审计）。
- [x] 基于角色与业务域的权限拦截；不设置县分、网格等数据范围字段。
- [x] 系统管理员可执行导入导出，并具备业务/网络提交、审核、应用权限；允许
  自提交、自审核，但必须完成审核确认和应用确认两个独立步骤。
- [x] 使用 `docs/政企专租线业务设备起底台账0811.xlsx` 做集成验证：3,113 行可进入
  暂存区，括号表头可识别，317 行阻断错误、2,796 行缺项、107 行重复/归属冲突。
- [x] 删除 `role_scopes` 模型与表，迁移现有数据库并更新预置角色权限。
- [ ] 上线前人工处理示例台账中的 317 行阻断错误和 107 行设备归属冲突，复核缺项
  报告，并执行导出、回传、审核、应用、备份恢复全流程演练。
- [x] 增加导入上传、暂存更正、双域审核、自审二次确认和扁平导出的 Web 路由测试
  （tests/test_import_web.py）。

## P0：先完成硬件闭环

- [ ] 在真实 `.env` 中设置强 `ADMIN_PASSWORD`（`SESSION_SECRET` 已为随机
  64 位；`ADMIN_PASSWORD` 当前为 8 位，操作者选择保持现状，上线前应人工轮换）。
- [x] 核对 A7670E 实际 AT 串口，并确认 `MODEM_PORT=/dev/ttyUSB1` 配置正确（AT
  握手、CSQ 29、CPIN READY、CEREG 注册均正常）。
- [x] 用 `aplay -l` 核对 3.5mm 输出设备，确认 `AUDIO_DEVICE=plughw:1,0`
  （rockchiprk809），本地播放 exit=0。
- [x] 准备格式兼容、音量合适的测试 WAV（8kHz/16bit/mono 识别性提示音，5.4s，
  使用操作者明确授权的测试号码完成实机验证）。
- [x] 人工运行 `uv run python scripts/hardware_smoke.py <测试号码> <wav>`：
  观察到 `VOICE CALL: BEGIN`，aplay exit=0，`VOICE CALL: END: 000005`，
  全程干净释放。
- [x] 应用级端到端：开启 `CALL_WORKER_ENABLED=1` + 管理页 Worker 开关，真实
  CallWorker 经 DB 拨号→接通→播放→挂断，15 条 CallEvent 完整落库，任务
  状态 `completed`、时长 15s（播放 5.4s + linger 10s 兜底后主动挂断）。
- [x] 对端听音确认：操作者确认手机上能听到完整测试音（验证 3.5mm→模块麦克风
  的音频通路）。
- [ ] 未接通、对端挂断、播放失败和连接超时后的实机释放验证（已有 fake
  serial 单测覆盖，实机待验）。
- [ ] 记录 TRRS 引脚、音量、声卡设备和可复现接线方案；若直连失败，评估转接、
  衰减、隔直或混音硬件。
## P0：完成单通道 Call Worker

- [x] 将 `ModemClient`、事件解析器和音频播放接入 `CallWorker`。
- [x] 实现 `queued -> dialing -> connected -> completed/short_call` 的成功路径。
- [x] 实现 `no_answer`、`rejected`、`cancelled_or_failed`、`busy`、`failed` 分类。
- [x] 使用 `CALL_CONNECT_TIMEOUT_SECONDS` 做应用层接通超时兜底。
- [x] 按 `MIN_CONNECTED_SECONDS` 判定 `short_call`，优先保存模块报告的时长。
- [x] 把 AT 命令、原始响应、URC、播放开始/结束、挂断和异常逐条写入
  `CallEvent`。
- [x] 确保异常和进程停止时尝试执行 `AT+CHUP`，并正确关闭串口和播放器。
- [x] 保证同一时刻只领取一个任务，避免 Web 多进程或重复 Worker 并发拨号。
- [x] 增加显式 `CALL_WORKER_ENABLED` 配置，默认关闭；只有确认硬件后才允许自动
  启动 Worker。
- [x] 真实硬件验收：按上文“先完成硬件闭环”步骤人工跑通一次完整外呼（smoke
  test + 应用级 CallWorker 端到端均完成，对端听音待操作者确认）。

## P0：重试与恢复

- [x] 对可重试的未接通结果最多重试一次，延迟使用 `RETRY_DELAY_SECONDS`。
- [x] 明确哪些状态可重试，避免对已接通短通话或人工终止进行自动重拨。
- [x] 为每次尝试保留独立、可追踪的任务/记录关系和 attempt 信息。
- [x] 测试重启后 `once` 任务标记为 `missed`，`cron` 不补打停机期间的历史次数。
- [x] 处理 Worker 启动时遗留在 `dialing` / `connected` 的任务，避免永久卡住。

## P1：测试与可靠性

- [x] 为计划时间计算、无效 cron 和时区转换增加单元测试（应用场景无夏令时
  corner case，不测 DST 边界；tests/test_plan_times.py）。
- [x] 为到期入队、暂停调度、立即拨打和重启恢复增加临时 SQLite 测试
  （tests/test_scheduler_flow.py、tests/test_plans.py）。
- [x] 为 Call Worker 增加 fake serial 和 fake audio 测试，覆盖成功、忙线、拒接、
  超时、播放失败和挂断。
- [x] 增加登录保护、客户/话术/计划表单校验和主要页面 smoke test（tests/test_web_ui.py、
  tests/test_web_auth.py）。
- [x] 修正删除被计划或通话记录引用的客户/话术时的用户可见错误处理（引用数量提示）。
- [x] 校验 `SESSION_SECRET`，启动时拒绝空值或示例值（tests/test_config.py）。
- [x] 增加结构化运行日志（app/logging.py，时间/级别/logger 格式），RedactFilter
  避免记录完整密钥和完整手机号（tests/test_logging.py）。

## 系统监控（/admin/system）

- [x] 组件状态 tab：调度器进程/最近扫描、外呼 Worker 进程/工作/当前任务/处理数、
  串口/音频/话术音频覆盖/数据库结构/任务队列/今日通话。
- [x] 调度器与 Worker 的运行时开关（`/settings/scheduler`、`/settings/worker`），
  Worker 受 `.env` 的 `CALL_WORKER_ENABLED` 硬开关约束。
- [x] 运行日志 tab：通话事件（可按事件类型筛选）+ 审计日志。
- [x] 相关设置 tab：非密钥环境配置与运行时设置展示。
- [x] 移除右上角调度器状态泡泡，入口统一收进“系统管理 → 系统监控”。
- [x] 鉴权：系统监控页（及现有 /admin/* 页面）统一加 `auth.require_permission`
  拦截（manage_config / manage_users / import / export 等，未授权 403）。
- [x] Worker 状态页在硬开关关闭时给出“配置未开启”引导；串口可用性采用只读
  探测（设备节点存在性 + Worker 最近运行结果，不打开串口、不发 AT）。

## P1：后台操作体验

- [x] 在通话列表增加状态、日期、客户筛选和分页。
- [x] 在通话详情完整展示时间线、原始串口行和错误信息。
- [x] 在计划表单中提供更清晰的 cron 校验错误和下一次执行时间预览。
- [x] 补全从客户或计划发起“立即拨打一次”的一致入口和确认提示。
- [x] 提供失败任务的人工重新入队操作，不修改原 cron 计划。
- [x] 在仪表盘显示 Worker 是否启用、串口是否可用及当前通话状态。
- [x] 对删除、拨号、重拨等高影响操作增加明确确认和结果反馈。

## P2：音频与部署

- [ ] 接入一个真实 TTS Provider，并保留 `TTS_PROVIDER=none` 作为离线默认值
  （需要 TTS API key，属人工配置项）。
- [x] 规范生成 WAV 的目录、命名、采样率、声道和缓存/覆盖策略（`data/audio/`，
  `script-{id}-{正文sha1前12位}.wav`，8kHz/16bit/mono，原子写覆盖，见
  app/audio.py 与 README「话术音频」）。
- [x] 增加音频生成失败状态（`tts_error` 记录原因）、重新生成入口与结果反馈，
  以及页面试听（登录后可读 `/audio/...` 只读路由，限 `data/audio/` 内 WAV，
  防路径穿越）。
- [x] 编写 Rock Pi 3A 部署文档（docs/deploy-rockpi.md：串口权限、声卡选择、
  `uv sync` 和服务启动）。
- [x] 提供 systemd 服务示例（scripts/systemd/：Web/Scheduler 与硬件 Worker
  的启动开关，Worker 由 `CALL_WORKER_ENABLED` 控制）。

## P1：备份与灾备

- [x] 实现 SQLite 一致性备份：使用 SQLite backup API 或等价安全方式，避免直接
  复制正在写入的数据库文件。
- [x] 将数据库备份、`data/imports/` 原始导入文件和 `data/audio/` 话术音频纳入
  备份范围；排除 `.env`、Session Secret、密码和其他运行时敏感凭据。
- [x] 增加可配置的定期备份任务：备份频率、保留天数、备份目录和失败重试次数均
  通过环境变量配置，默认保留本地最近备份。
- [x] 增加 WebDAV 远端备份：支持 HTTPS、目标目录、账号和应用专用密码配置；
  凭据只从环境变量读取，不写入数据库、日志或备份清单。
- [x] 上传前对备份包做完整性校验，传输失败时保留本地待上传文件，并在系统监控
  页面显示最近成功时间、失败原因和远端状态。
- [x] 设计备份命名、版本和保留策略，避免远端无限增长，并防止同一备份被重复覆盖。
- [x] 提供管理员手动触发备份、下载本地备份和查看备份历史的入口，并记录审计日志。
- [x] 完成恢复演练：从仅有数据库备份恢复客户/台账/外呼计划/通话记录，再从包含
  `data/imports/` 与 `data/audio/` 的完整备份恢复导入追溯和话术播放链路。
- [x] 验证断电、数据库损坏、WebDAV 不可用、磁盘空间不足和恢复中断等失败路径。

## 明确延期

- [ ] ASR、实时转写和录音。
- [ ] LLM 实时对话、抢断和动态话术。
- [ ] DTMF 按键反馈。
- [ ] 节假日、工作日历和复杂补偿策略。
- [ ] 公网部署安全加固；当前多用户、角色和业务域权限仅面向内网使用。

以上项目不进入最小 Demo，除非产品范围被明确调整。

## 最小 Demo 验收

- [x] 管理员可登录并管理客户、话术和 `once` / `cron` 回访计划。
- [x] 到期计划只生成一次正确排序的任务，暂停调度时不生成新任务。
- [x] 单通道 Worker 能拨号、识别接通、播放 WAV、挂断并释放资源。
- [x] 成功、短通话、未接通、忙线、超时和播放失败均有正确主状态。
- [x] 每次通话可在后台查看完整时间、时长、错误和串口事件。
- [x] 自动重试不超过配置上限，服务重启不会补打历史任务。
- [x] 自动化测试通过（154 项），真实硬件链路由操作者完成端到端验收（smoke +
  应用级 E2E + 对端听音）；未接通/挂断等实机释放路径仍待人工补验。
